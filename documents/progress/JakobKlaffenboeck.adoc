:toc:

= Jakob Klaffenboeck - Progess

== 9-12-2024 Progress

=== Speech to Text implementation Research

=== HTML - simple buttons idea

[source, html]
----

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Speech-to-Text Example</title>
</head>
<body>
    <h1>Speech-to-Text Example</h1>
    <button id="start">Start Recording</button>
    <button id="stop">Stop Recording</button>
    <p id="output">Transcribed Text: <span></span></p>
    <script src="app.js"></script>
</body>
</html>


----

=== Java Script potential solution

- Using web speech API
- Demonstration of Web Speech Recognition API vs Speechly:
- link: https://www.youtube.com/watch?v=1hcdCrFl-MQ


[source, sql]
----
const startButton = document.getElementById('start');
const stopButton = document.getElementById('stop');
const output = document.getElementById('output').querySelector('span');

let recognition;

if ('webkitSpeechRecognition' in window || 'SpeechRecognition' in window) {
    const SpeechRecognition = window.SpeechRecognition || window.webkitSpeechRecognition;
    recognition = new SpeechRecognition();

    recognition.lang = 'en-US'; // Set language
    recognition.interimResults = true;

    recognition.onresult = (event) => {
        const transcript = Array.from(event.results)
            .map(result => result[0])
            .map(result => result.transcript)
            .join('');
        output.textContent = transcript;
    };

    recognition.onerror = (event) => {
        console.error('Speech Recognition Error:', event.error);
    };
} else {
    alert('Your browser does not support Speech Recognition.');
}

startButton.addEventListener('click', () => {
    if (recognition) recognition.start();
});

stopButton.addEventListener('click', () => {
    if (recognition) recognition.stop();
});

----


== 13-1-2025 Progress

=== Recognising when mic is recording

[source, javascript]
----
    recognition.onstart = () => {
        console.log('Speech recognition started');
        // Perform an action when recording starts
        document.body.style.backgroundColor = 'lightblue'; // Example action
    };

    recognition.onend = () => {
        console.log('Speech recognition ended');
        // Perform an action when recording ends
        document.body.style.backgroundColor = ''; // Example action
    };

----

== 15-01-2025 Progress

=== Chat History

[source, javascript]
----

function animateChat(positionLength) {
    const queryOutput = document.getElementById("queryOutput");
    const chatHistory = document.getElementById("chatHistory");
    const userInput = document.getElementById("userInput");

    if (positionLength === 0) {
        queryOutput.classList.add("hidden");
        return;
    }

    // Create a new chat entry
    const chatEntry = document.createElement("div");
    chatEntry.classList.add("chatEntry");

    // Add user query
    const userQuery = document.createElement("p");
    userQuery.classList.add("userQuery");
    userQuery.textContent = userInput.value;
    chatEntry.appendChild(userQuery);

    // Add search results
    const resultsContainer = document.createElement("div");
    resultsContainer.classList.add("resultsContainer");
    resultsContainer.innerHTML = queryOutput.innerHTML;
    chatEntry.appendChild(resultsContainer);

    // Append the chat entry to the chat history
    chatHistory.appendChild(chatEntry);

    // Clear the input field
    userInput.value = "";

    queryOutput.classList.add("hidden");
}


----

[source, html]
----
<div id="chatHistory"></div>
----

[source, css]
----
#chatHistory {
    width: 100%;
    max-height: 50vh;
    overflow-y: auto;
    margin-bottom: 1em;
}

.chatEntry {
    margin-bottom: 1em;
    padding: 1em;
    background-color: #f9f9f9;
    border-radius: 0.5em;
}

.userQuery {
    font-weight: bold;
    margin-bottom: 0.5em;
}

.resultsContainer {
    background-color: #D9D9D9;
    padding: 1em;
    border-radius: 0.5em;
}
----

== 30-01-2025

=== LLM evaluation

- DeepSeek
- ChatGPT
- Mistral

==== Different Models Docs:
- https://api-docs.deepseek.com/
- https://chatgpt.com/g/g-VTUNFyGKk-gpt-api-key-assistance
- https://docs.aicontentlabs.com/articles/mistral-ai-api-key/
- https://console.mistral.ai/api-keys/

=== Prices
- ChatGPT 3.5: 8k context: ~$0.0015/1k tokens (input), ~$0.002/1k tokens (output).
- DeepSeek: Gratis bei lokalem Hosting des Service, sonst $0.014 = 1M Tokens (input),  $0.28 = 1M Tokens (output)
* https://api-docs.deepseek.com/quick_start/pricing
- Mistral:
* https://mistral.ai/technology/#pricing

== Prompt Engineering

- 4. Clarification and Transparency: If the document provides information that might be different or context-specific (e.g., boiling point of water in a specific location), include this context in your response to ensure accuracy.


== TS Umstieg

Im build.sh

1. frontend builden
2. target in das Meta Inf kopieren
3. quarkus starten